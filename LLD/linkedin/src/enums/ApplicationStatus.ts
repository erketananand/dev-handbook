/**
 * Enumeration for job application statuses
 */
export enum ApplicationStatus {
  APPLIED = "APPLIED",
  REVIEWING = "REVIEWING",
  SHORTLISTED = "SHORTLISTED",
  REJECTED = "REJECTED",
  ACCEPTED = "ACCEPTED",
}
