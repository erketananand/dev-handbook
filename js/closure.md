### Closure:
A **closure** is a function that **remembers** the variables from its outer (enclosing) scope even after the outer function has finished executing. For async code, closure is used very frequently.
